

//  This is (Simprint.js) what gets called first and then kicks off Cordova to call our native code. */ 

// Let's start with the JavaScript API that we’d like to expose to the developer.
// In this case, our JavaScript code lives in www/Simprint.js.



/*
var exec = require('cordova/exec');

var PLUGIN_NAME = 'Simprint';

var Simprint = {
  echo: function(phrase, cb) {
    exec(cb, null, PLUGIN_NAME, 'echo', [phrase]);
  }

 :
 :
  
};

module.exports = Simprint; */
